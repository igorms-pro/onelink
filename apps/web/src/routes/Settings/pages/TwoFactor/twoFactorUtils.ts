// Legacy twoFactor utilities (TOTP helpers).
// These were used by the old custom 2FA implementation that stored
// encrypted secrets in the user_2fa table. They are now unused since
// we rely on Supabase MFA and are kept only temporarily for reference.

export {};
