import { test } from "@playwright/test";

// Placeholder: will be enabled after SQL is applied and data is seeded
test.skip("public slug renders scheduled links only", async ({ page }) => {
  await page.goto("http://localhost:5173/u/igor");
});


