# Guide de Création - Vidéo "How It Works"

Ce document décrit le contenu et la structure de la vidéo tutorielle pour la section "How It Works" de la landing page.

## 📍 Emplacement

La vidéo sera intégrée dans la section **"How It Works"** de la landing page (`apps/landing/src/components/sections/HowItWorksSection.tsx`).

## 🎯 Objectif de la Vidéo

La vidéo doit être un **teaser rapide et accrocheur** qui montre OneLink en action, pas un tutoriel détaillé. L'objectif est de donner envie et de montrer la simplicité du produit.

**Durée recommandée**: **45-90 secondes maximum** (idéalement 60 secondes)

---

## 📋 Structure de la Vidéo (4 Étapes Rapides)

**Format**: Montage rapide avec transitions dynamiques, pas de narration longue. Montrer l'essentiel de chaque étape en 10-15 secondes.

### **Étape 1: Sign Up** (10-15 secondes)
**Ce qu'il faut montrer (rapide):**
- Capture d'écran de la page d'inscription
- Clic sur "Sign Up" avec Google (le plus rapide)
- Animation rapide vers le dashboard
- **Message visuel**: "Gratuit • Rapide • Simple"

**Éléments visuels:**
- Transition rapide (1-2 secondes par écran)
- Zoom sur le bouton "Sign Up with Google"
- Animation de chargement courte

---

### **Étape 2: Create Your Link** (15-20 secondes)
**Ce qu'il faut montrer (rapide):**
- Dashboard avec animation d'apparition
- Clic sur "Add Link" → formulaire s'ouvre
- Remplissage rapide (titre + URL) en accéléré
- Sauvegarde → lien apparaît dans la liste
- **Optionnel**: Upload rapide d'un fichier (drag & drop en 2 secondes)
- **Message visuel**: "Liens + Fichiers en quelques clics"

**Éléments visuels:**
- Accélération du remplissage du formulaire (x2 ou x3)
- Animation de confirmation (checkmark, toast)
- Montrer plusieurs liens créés rapidement

---

### **Étape 3: Share** (10-15 secondes)
**Ce qu'il faut montrer (rapide):**
- Le profil public OneLink (ex: `getonelink.io/username`)
- Zoom sur le lien unique
- Clic sur "Copy Link" → copié
- Montage rapide de 3-4 exemples d'utilisation:
  - Bio Instagram (1 seconde)
  - Tweet avec le lien (1 seconde)
  - Signature email (1 seconde)
  - QR code (1 seconde)
- **Message visuel**: "Un seul lien partout"

**Éléments visuels:**
- Split screen ou montage rapide des exemples
- Animation "Copied!" visible
- Le profil doit être beau et professionnel

---

### **Étape 4: Track** (10-15 secondes)
**Ce qu'il faut montrer (rapide):**
- Retour au Dashboard
- Zoom sur la section Analytics
- Graphiques animés (chiffres qui montent rapidement)
- Montrer: clics, vues, téléchargements
- **Message visuel**: "Statistiques en temps réel"

**Éléments visuels:**
- Animation des chiffres qui augmentent
- Graphiques colorés et visibles
- Interface moderne et claire

---

## 🎬 Conseils de Production

### **Style et Ton**
- **Ton**: Énergique, dynamique, moderne
- **Rythme**: **TRÈS RAPIDE** - transitions toutes les 1-2 secondes
- **Musique**: Beat énergique, rythmé, moderne (hip-hop léger, electronic, pop)
- **Pas de narration longue** - textes à l'écran uniquement ou voix-off très courte

### **Qualité Vidéo**
- **Résolution**: Minimum 1080p (Full HD), idéalement 4K
- **Format**: 16:9 (aspect ratio standard YouTube)
- **FPS**: 30fps minimum, 60fps idéal pour les captures d'écran
- **Audio**: Voix claire, pas d'écho, musique de fond discrète

### **Captures d'Écran**
- Utiliser un outil de capture d'écran professionnel (OBS, ScreenFlow, Camtasia)
- Zoomer sur les éléments importants (curseur, boutons, textes)
- Ajouter des annotations si nécessaire (flèches, surlignages)
- Montrer les transitions fluides entre les pages

### **Montage**
- **Transitions rapides et dynamiques** (cut, zoom, slide)
- Textes à l'écran courts et percutants pour chaque étape:
  - "1. Inscription" (2 secondes)
  - "2. Créer" (2 secondes)
  - "3. Partager" (2 secondes)
  - "4. Suivre" (2 secondes)
- Call-to-action final (5 secondes): "Créez votre compte gratuit →"
- **Rythme**: Chaque étape = 10-15 secondes max

---

## 📝 Script Voix-Off en Anglais (60 secondes)

### **Timing Détaillé avec Visuels**

#### **0-3 secondes - Introduction**
**Montrer:**
- Logo OneLink animé (fade in)
- Tagline "One link. Multiple lives." apparaît

**Dire:**
> "OneLink. One link. Multiple lives."

---

#### **3-15 secondes - Étape 1: Sign Up**
**Montrer:**
- Transition vers la page d'inscription (`app.getonelink.io` ou landing page)
- Zoom sur le bouton "Sign Up" ou "Get Started Free"
- Clic sur "Sign Up with Google" (le plus rapide)
- Animation rapide: OAuth popup → Dashboard apparaît
- Texte à l'écran: "FREE" (badge animé)

**Dire:**
> "Start with a free account. Sign up in seconds with Google, and you're in."

---

#### **15-30 secondes - Étape 2: Create Your Link**
**Montrer:**
- Dashboard OneLink (vue d'ensemble)
- Clic sur "Add Link" ou "New Link"
- Formulaire s'ouvre
- Remplissage accéléré (x2): Titre "My Portfolio" + URL "https://..."
- Sauvegarde → Le lien apparaît dans la liste
- Optionnel: Upload rapide d'un fichier (drag & drop en 2 secondes)
- Texte à l'écran: "Links + Files"

**Dire:**
> "Add your links and files. Give them a title, paste the URL, and you're done. Upload files with a simple drag and drop."

---

#### **30-45 secondes - Étape 3: Share**
**Montrer:**
- Le profil public OneLink s'affiche (ex: `getonelink.io/username`)
- Zoom sur le lien unique OneLink
- Clic sur "Copy Link" → Animation "Copied!"
- Montage rapide (split screen ou séquence):
  - Bio Instagram avec le lien (1 seconde)
  - Tweet avec le lien (1 seconde)
  - Signature email (1 seconde)
- Texte à l'écran: "Share Everywhere"

**Dire:**
> "Your OneLink is ready. Copy your unique link and share it everywhere—Instagram bio, Twitter, email signature. One link, multiple lives."

---

#### **45-57 secondes - Étape 4: Track**
**Montrer:**
- Retour au Dashboard
- Zoom sur la section Analytics
- Graphiques animés (chiffres qui montent rapidement):
  - Total clicks: 0 → 150 (animation)
  - Profile views: 0 → 89 (animation)
  - Downloads: 0 → 23 (animation)
- Graphique en barres ou ligne qui se remplit
- Texte à l'écran: "Real-time Analytics"

**Dire:**
> "Track everything in real-time. See clicks, views, and downloads. Get insights to grow your audience."

---

#### **57-60 secondes - Call-to-Action**
**Montrer:**
- Écran final avec logo OneLink
- Texte à l'écran: "Create Your Free Account →" (avec flèche animée)
- Badge "FREE" visible
- URL: `getonelink.io` ou `app.getonelink.io`
- Tagline finale: "One link. Multiple lives." (petit, en bas)

**Dire:**
> "Create your free account today. OneLink. Multiple Lives."

---

## 🎤 Notes pour la Voix-Off

### **Ton et Style**
- **Ton**: Énergique, confiant, mais pas agressif
- **Rythme**: Rapide mais clair (pas de précipitation)
- **Pauses**: Courtes pauses entre les étapes (0.5-1 seconde)
- **Emphase**: Mettre l'accent sur "FREE", "seconds", "real-time", "everything"

### **Prononciation**
- "OneLink" = "Wun-Link" (clair et distinct)
- Parler naturellement, comme si vous expliquiez à un ami
- Éviter le ton "commercial" trop poussé

### **Timing Total**
- **Total**: 60 secondes exactement
- Chaque étape: ~12-15 secondes
- CTA final: 3 secondes

---

## 📋 Script Complet (Version Texte)

```
[0-3s]
"OneLink. One link. Multiple lives."

[3-15s]
"Start with a free account. Sign up in seconds with Google, and you're in."

[15-30s]
"Add your links and files. Give them a title, paste the URL, and you're done. Upload files with a simple drag and drop."

[30-45s]
"Your OneLink is ready. Copy your unique link and share it everywhere—Instagram bio, Twitter, email signature. One link, multiple lives."

[45-57s]
"Track everything in real-time. See clicks, views, and downloads. Get insights to grow your audience."

[57-60s]
"Create your free account today. OneLink—one link to share everything."
```

---

## 🎬 Checklist de Synchronisation

- [ ] Voix-off enregistrée en 60 secondes exactement
- [ ] Chaque section visuelle correspond au timing de la voix
- [ ] Transitions visuelles synchronisées avec les pauses dans la voix
- [ ] Textes à l'écran apparaissent au bon moment
- [ ] Musique de fond ne couvre pas la voix (volume ajusté)
- [ ] Test final: visionner avec voix + visuels pour vérifier la synchronisation

---

## 🎨 Éléments Visuels Recommandés

### **Thumbnail YouTube**
- Titre: "OneLink - One link. Multiple lives."
- Image: Capture d'écran du dashboard avec overlay "FREE"
- Couleurs: Purple/Blue (cohérent avec la marque)
- Tagline visible: "One link. Multiple lives."

### **Intro/Outro**
- Logo OneLink animé
- Tagline: "One link. Multiple lives."
- Call-to-action: "Try it free"

### **Annotations à l'Écran**
- Flèches pointant vers les boutons importants
- Surlignage des champs de formulaire
- Badges "GRATUIT" ou "RAPIDE" sur certaines actions
- Compteur de temps ("Étape 1/4", "Étape 2/4", etc.)

---

## ✅ Checklist Avant Publication

- [ ] Vidéo dure **45-90 secondes maximum** (idéalement 60 secondes)
- [ ] Toutes les 4 étapes sont couvertes rapidement (10-15s chacune)
- [ ] Rythme rapide et dynamique (pas de temps mort)
- [ ] Qualité vidéo: 1080p minimum
- [ ] Musique énergique et moderne
- [ ] Textes à l'écran courts et percutants pour chaque étape
- [ ] Call-to-action clair à la fin (5 secondes)
- [ ] Thumbnail attractif créé
- [ ] Description YouTube avec liens vers le site
- [ ] Tags YouTube: "onelink", "link in bio", "portfolio", "social media"
- [ ] Vidéo testée sur mobile et desktop
- [ ] Pas de narration longue (ou voix-off très courte)

---

## 🔗 Intégration Technique

Une fois la vidéo créée et uploadée sur YouTube:

1. **Récupérer l'ID de la vidéo YouTube**
   - Exemple: Si l'URL est `https://www.youtube.com/watch?v=dQw4w9WgXcQ`
   - L'ID est: `dQw4w9WgXcQ`

2. **Ajouter l'ID dans le code**
   - Option 1: Variable d'environnement (recommandé)
     ```bash
     VITE_YOUTUBE_HOW_IT_WORKS_ID=dQw4w9WgXcQ
     ```
   - Option 2: Directement dans le code
     ```typescript
     const YOUTUBE_VIDEO_ID = "dQw4w9WgXcQ";
     ```

3. **Tester l'intégration**
   - Vérifier que la vidéo s'affiche correctement
   - Tester sur mobile et desktop
   - Vérifier que les paramètres YouTube sont corrects (`rel=0&modestbranding=1&showinfo=0`)

---

## 📊 Métriques de Succès

Après publication, suivre:
- **Taux de visionnage**: Combien de visiteurs regardent la vidéo
- **Taux de complétion**: Avec une vidéo courte (60s), le taux devrait être élevé (>70%)
- **Taux de conversion**: Combien s'inscrivent après avoir vu la vidéo
- **Engagement**: Commentaires, likes, partages sur YouTube
- **Temps moyen de visionnage**: Devrait être proche de la durée totale (60s)

---

## 🎯 Messages Clés à Communiquer (Rapides)

1. **Gratuit**: "Gratuit" (visible dès le début)
2. **Rapidité**: "En 2 minutes" ou "Rapide"
3. **Simplicité**: "Simple" ou "Facile"
4. **Polyvalence**: "Un seul lien pour tout"
5. **Professionnel**: "Statistiques en temps réel"

**Format**: Textes courts à l'écran, pas de phrases longues

---

## 🎨 Préparation du Profil pour la Vidéo

### **Profil à Créer (Important!)**

Pour que la vidéo soit convaincante, il faut créer un **profil complet et professionnel** avec de vraies données réalistes.

#### **Compte à Créer**
- **Username**: Quelque chose de professionnel (ex: `johndoe`, `portfolio`, `creator`)
- **Bio**: Description accrocheuse (ex: "Creator • Designer • Sharing my work")
- **Photo de profil**: Image professionnelle ou logo
- **Thème**: Design cohérent et moderne

#### **Liens à Ajouter (Minimum 5-7 liens)**
1. **Portfolio** → `https://portfolio.example.com`
2. **GitHub** → `https://github.com/username`
3. **LinkedIn** → `https://linkedin.com/in/username`
4. **Twitter/X** → `https://twitter.com/username`
5. **Instagram** → `https://instagram.com/username`
6. **Blog** → `https://blog.example.com`
7. **Ressource/Download** → `https://example.com/resource`

**Conseil**: Utiliser de vrais liens qui fonctionnent (même si ce sont des exemples)

#### **Fichiers/Drops à Créer (2-3 drops)**
1. **Drop 1**: Portfolio PDF (ex: "My Portfolio 2024.pdf")
2. **Drop 2**: Ressources multiples (ex: 3-4 fichiers: templates, guides, etc.)
3. **Drop 3**: Media kit (ex: logos, images, brand assets)

#### **Statistiques à Préparer (Réalistes!)**
Pour la section Analytics, il faut des chiffres qui ont du sens:

**Total Clicks**: 150-300 clics
- Répartition par lien:
  - Portfolio: 45 clics
  - GitHub: 38 clics
  - LinkedIn: 52 clics
  - Twitter: 28 clics
  - Instagram: 35 clics
  - Blog: 22 clics
  - Ressource: 18 clics

**Profile Views**: 89-120 vues

**Downloads**: 23-45 téléchargements
- Portfolio PDF: 12 downloads
- Ressources: 18 downloads
- Media kit: 15 downloads

**Graphiques**: 
- Courbe de croissance sur 7-14 jours
- Pic d'activité récent (derniers jours)
- Répartition par source (si disponible)

#### **Design du Profil Public**
- **Layout**: Professionnel, bien organisé
- **Couleurs**: Cohérentes avec la marque OneLink (purple/blue)
- **Ordre des liens**: Logique (Portfolio en premier, puis réseaux sociaux)
- **Visuel**: Attractif, moderne, inspire confiance

#### **Checklist Avant Tournage**
- [ ] Compte créé avec username professionnel
- [ ] 5-7 liens ajoutés avec de vrais URLs
- [ ] 2-3 drops créés avec fichiers réels
- [ ] Bio complète et accrocheuse
- [ ] Photo de profil professionnelle
- [ ] Statistiques générées (clics, vues, downloads)
- [ ] Profil public testé et vérifié (belle apparence)
- [ ] Dashboard avec données réalistes
- [ ] Graphiques Analytics avec courbes visibles

#### **Comment Générer les Statistiques (Option 3: Direct DB - Recommandé!)**

**Méthode rapide**: Modifier directement en base de données pour avoir des stats réalistes instantanément.

**Tables à modifier:**
- `public.link_clicks` - pour les clics sur les liens
- `public.drop_views` - pour les vues des drops  
- `public.file_downloads` - pour les téléchargements

**Script SQL à exécuter dans Supabase SQL Editor:**

```sql
-- ============================================
-- GÉNÉRATION DE STATISTIQUES POUR LA VIDÉO
-- ============================================
-- Remplacez 'YOUR_PROFILE_ID' et 'YOUR_USER_ID' par les vrais IDs

-- 1. Générer des clics sur les liens (répartis sur 14 jours)
-- Total: ~150-300 clics répartis sur les liens

WITH link_ids AS (
  SELECT id, label FROM public.links 
  WHERE profile_id = 'YOUR_PROFILE_ID'::uuid
  ORDER BY "order"
),
click_distribution AS (
  SELECT 
    id as link_id,
    CASE 
      WHEN label ILIKE '%portfolio%' THEN 45
      WHEN label ILIKE '%github%' THEN 38
      WHEN label ILIKE '%linkedin%' THEN 52
      WHEN label ILIKE '%twitter%' OR label ILIKE '%x%' THEN 28
      WHEN label ILIKE '%instagram%' THEN 35
      WHEN label ILIKE '%blog%' THEN 22
      ELSE 18
    END as clicks_count
  FROM link_ids
)
INSERT INTO public.link_clicks (link_id, clicked_at, user_id, ip_address)
SELECT 
  cd.link_id,
  NOW() - (random() * interval '14 days') as clicked_at,
  NULL as user_id, -- anonymous clicks
  ('192.168.' || floor(random() * 255)::text || '.' || floor(random() * 255)::text)::inet as ip_address
FROM click_distribution cd
CROSS JOIN generate_series(1, cd.clicks_count) as gs;

-- 2. Générer des vues de profil (89-120 vues sur 14 jours)
-- Note: Les vues de profil ne sont pas directement trackées, mais on peut créer des vues de drops

WITH drop_ids AS (
  SELECT id FROM public.drops 
  WHERE profile_id = 'YOUR_PROFILE_ID'::uuid
  LIMIT 3
)
INSERT INTO public.drop_views (drop_id, viewed_at, user_id, ip_address)
SELECT 
  d.id,
  NOW() - (random() * interval '14 days') as viewed_at,
  NULL as user_id,
  ('192.168.' || floor(random() * 255)::text || '.' || floor(random() * 255)::text)::inet as ip_address
FROM drop_ids d
CROSS JOIN generate_series(1, 30) as gs; -- ~30 vues par drop = 90 total

-- 3. Générer des téléchargements (23-45 downloads)
WITH submission_ids AS (
  SELECT s.id, s.drop_id 
  FROM public.submissions s
  JOIN public.drops d ON d.id = s.drop_id
  WHERE d.profile_id = 'YOUR_PROFILE_ID'::uuid
  LIMIT 5
)
INSERT INTO public.file_downloads (submission_id, file_path, downloaded_at, user_id, ip_address)
SELECT 
  s.id,
  s.file_path,
  NOW() - (random() * interval '14 days') as downloaded_at,
  NULL as user_id,
  ('192.168.' || floor(random() * 255)::text || '.' || floor(random() * 255)::text)::inet as ip_address
FROM submission_ids s
CROSS JOIN generate_series(1, 8) as gs; -- ~8 downloads par fichier = 40 total

-- 4. Vérifier les résultats
SELECT 
  'Link Clicks' as metric,
  COUNT(*)::int as total
FROM public.link_clicks c
JOIN public.links l ON l.id = c.link_id
WHERE l.profile_id = 'YOUR_PROFILE_ID'::uuid

UNION ALL

SELECT 
  'Drop Views' as metric,
  COUNT(*)::int as total
FROM public.drop_views v
JOIN public.drops d ON d.id = v.drop_id
WHERE d.profile_id = 'YOUR_PROFILE_ID'::uuid

UNION ALL

SELECT 
  'File Downloads' as metric,
  COUNT(*)::int as total
FROM public.file_downloads fd
JOIN public.submissions s ON s.id = fd.submission_id
JOIN public.drops d ON d.id = s.drop_id
WHERE d.profile_id = 'YOUR_PROFILE_ID'::uuid;
```

**Instructions:**
1. Connectez-vous à Supabase Dashboard → SQL Editor
2. Remplacez `'YOUR_PROFILE_ID'` par l'UUID de votre profil (trouvable dans la table `profiles`)
3. Exécutez le script
4. Vérifiez les résultats avec la dernière requête
5. Les stats apparaîtront immédiatement dans le dashboard!

**Pour trouver votre Profile ID:**
```sql
SELECT id, slug, display_name 
FROM public.profiles 
WHERE user_id = auth.uid();
```

**Important**: 
- Les statistiques sont réparties sur les 14 derniers jours pour avoir des courbes réalistes
- Les clics sont anonymes (`user_id = NULL`) pour éviter les problèmes de self-clicks
- Les IPs sont générées aléatoirement pour avoir l'air réaliste
- Après la vidéo, vous pouvez supprimer ces données si besoin avec `DELETE FROM public.link_clicks WHERE ...`

---

## 📝 Notes Additionnelles

- **Format**: Vidéo rapide et accrocheuse, pas un tutoriel détaillé
- La vidéo doit être **multilingue** ou avoir des sous-titres dans plusieurs langues (EN, FR, ES, etc.)
- Penser à créer des **versions encore plus courtes** (15-30 secondes) pour les réseaux sociaux (Instagram Reels, TikTok, YouTube Shorts)
- La vidéo peut être mise à jour si de nouvelles fonctionnalités sont ajoutées
- Garder la vidéo à jour avec l'interface actuelle de l'application
- **Style**: Inspiré des vidéos produit modernes (Apple, Stripe, Linear) - rapide, épuré, efficace
- **Profil**: Créer un profil complet et professionnel AVANT de tourner la vidéo

---

**Dernière mise à jour**: Janvier 2026
